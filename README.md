# Example of code block
 
    ```javascript   
    const leftMd = leftMdArr.filter(item => item === markdown)  
    ``` 
     
```javascript   
const leftMd = leftMdArr.filter(item => item === markdown)  
``` 
# This is the code
 
```const leftMd = leftMdArr.filter(item => item === markdown)   
        }const leftMd = leftMdArr.filter(item => item === markdown)  
``` 
